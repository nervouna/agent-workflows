#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for commits in codex-maintenance." >&2
  echo "Install it with: brew install gitleaks" >&2
  exit 1
fi

gitleaks protect --staged --redact
